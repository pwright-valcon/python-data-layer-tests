# python-data-layer-tests

### Run locally

* Export environment variables listed in `config.py` for Postgres DB connection
* Install requirements: `pip3 install -r requirements.txt`
* Run `python main.py`

### Run tests

* Install requirements: `pip3 install -r requirements.txt`
* Run `python -m unittest`
